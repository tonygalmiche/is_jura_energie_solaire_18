<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Restreindre le menu "Apps" (menu_management) au groupe admin uniquement -->
    <record id="base.menu_management" model="ir.ui.menu">
        <field name="groups_id" eval="[Command.set([ref('base.group_system')])]"/>
    </record>

    <!-- Restreindre le menu "Employés" (menu_hr_root) au groupe hr.group_hr_user uniquement -->
    <record id="hr.menu_hr_root" model="ir.ui.menu">
        <field name="groups_id" eval="[Command.set([ref('hr.group_hr_user')])]"/>
    </record>

    <!-- Restreindre le menu "Contacts" au groupe sales_team.group_sale_salesman_all_leads uniquement -->
    <record id="contacts.menu_contacts" model="ir.ui.menu">
        <field name="groups_id" eval="[Command.set([ref('sales_team.group_sale_salesman_all_leads')])]"/>
    </record>

    <!-- Restreindre le menu "Calendrier" au groupe sales_team.group_sale_salesman_all_leads uniquement -->
    <record id="calendar.mail_menu_calendar" model="ir.ui.menu">
        <field name="groups_id" eval="[Command.set([ref('sales_team.group_sale_salesman_all_leads')])]"/>
    </record>

    <!-- Restreindre le menu "Discussion" au groupe sales_team.group_sale_salesman_all_leads uniquement -->
    <record id="mail.menu_root_discuss" model="ir.ui.menu">
        <field name="groups_id" eval="[Command.set([ref('sales_team.group_sale_salesman_all_leads')])]"/>
    </record>

    <!-- Restreindre le menu "To-do" au groupe sales_team.group_sale_salesman_all_leads uniquement -->
    <record id="project_todo.menu_todo_todos" model="ir.ui.menu">
        <field name="groups_id" eval="[Command.set([ref('sales_team.group_sale_salesman_all_leads')])]"/>
    </record>
</odoo>
